# Max — Memoria operativa

> File aggiornato dall'agente Max stesso al termine di ogni task.
> Max legge questo file all'inizio di ogni sessione per recuperare contesto.

## Decisioni recenti (max 20, FIFO)

- 2026-05-14: Avviato refactor radicale GUI + sistema agenti. Piano completo in `/root/.claude/plans/la-gui-pessima-delegated-bentley.md`.
- 2026-05-14: Accettata sfida utente "GUI perfetta o mobile". Definiti criteri di accettazione misurabili + gate review dopo fase 3.
- 2026-05-14: Decisi modelli agenti — Max=opus, Paky/Tom/Chloe/Marco=sonnet.
- 2026-05-14: GUI Fase 1 completata (PR #11, #12 mergiati). Demo TopBar + DashboardWorkspace + 5 info widget MVP + dark.qss 1203 LOC.
- 2026-05-14: Bug fix QShortcut (PR #13 mergiato) — `QShortcut` sta in `PyQt6.QtGui`, NON `QtWidgets`. Aggiunto quality gate non derogabile agli agent Paky/Marco: real import test + istanziazione widget prima di chiudere ogni task GUI.
- 2026-05-14: **Gate review SUPERATO** — utente ha lanciato l'app, conferma "così mi piace". Qt vince, Path B mobile non attivato. Procedere con Fase 1.5 Polish (3 ondate) poi Fasi 2-6.
- 2026-05-14: Difetti gate review documentati in `docs/SPRINT.md` (15 difetti, 3 ondate). Punto di ripartenza per nuova sessione: Fase 1.5 Ondata 1 (Marco).
- 2026-05-14: Aggiunto requisito utente i18n — IT default + EN opzionale, lasciare in inglese solo nomi tecnici riconosciuti (AI, ML, Hurst, Kelly, ATR, long/short, watchlist, broker, ecc.). Tabella di mapping completa con ~50 chiavi + tooltip esplicativi in `docs/SPRINT.md` Fase 1.6. Architettura: dict Python puro (no gettext/Qt Linguist), `gui/i18n/strings.py` con `tr()`, persistenza in QSettings, selettore in SettingsWorkspace.
- 2026-05-15: **Fase 1.5 COMPLETATA** — commit `f92bda2` consolida 15 fix gate review (Ondate 1+2+3): layout TopBar+WATCHLIST, header colonne posizioni, _GaugeCard border, regime/hurst da AppState (no divergenze), _AIPanel restyled con hit/miss sparkline, KPIBadge tooltip anchor, icon.png 256x256 candela verde, BrokerPill bullet U+2022. Quality gate import OK. Path venv su questo Windows: `.\.venv312\Scripts\python.exe` (NON `.venv`). Classe MainWindow è `TradingMainWindow` (non `MainWindow`).
- 2026-05-15: Avviata Fase 1.6.1 — audit testi UI hardcoded delegato a Marco. Visual check Fase 1.5 demandato all'utente al prossimo lancio app (non bloccante per audit read-only).
- 2026-05-16: **Fase 1.6.2 + 1.6.3 COMPLETATE**. Commit `97e40d9` (Paky) crea `gui/i18n/strings.py` con dict IT/EN da 136 chiavi, funzione `tr()` con fallback alla chiave e interpolazione `.format()`, bootstrap QSettings in `gui/app.py`, setter `AppState.language` con segnale `language_changed`. Commit `2357c01` (Marco) refactor `tr()` nei 4 file GUI principali. Commit `b8239b8` (Marco) refactor `tr()` sui 8 panel atomici (~39 sostituzioni) — nessuna chiave nuova necessaria, già pre-create da Paky. Quality gate cross-validato (Marco+Max indipendenti): import test 8/8, istanziazione headless, test cambio lingua IT->EN runtime su BrokerPanel ("Salva impostazioni" -> "Save settings"). **Fase 1.6 Step 3 chiuso al 100%.** Resta solo Step 4 (selettore lingua in SettingsWorkspace) → fatto in Fase 2.
- 2026-05-16: **Fase 2 COMPLETATA** — commit `810387a`. 5 nuovi workspace creati in parallelo Marco+Paky (1285 LOC totali): `OrderTicketWorkspace` (QSplitter 3 col: form ordini + tabella + BrokerPanel), `AnalysisWorkspace` (QSplitter nested V/H: AI+Chart+Data), `BacktestWorkspace` (full-width BacktestPanel), `PatternsWorkspace` (Pattern+Chart H-split), `SettingsWorkspace` (QScrollArea con 4 sezioni: lingua/broker/risk/info). `main_window.py` esteso (121→185 LOC) con 6-workspace QStackedWidget + Ctrl+1..6 shortcut + persistenza QSettings (workspace attivo + window_geometry) + statusbar tradotta. **Step 4 di Fase 1.6 chiuso**: selettore lingua IT/EN in SettingsWorkspace salva in QSettings + applica AppState.language + dialog "Riavvia per applicare". 187 chiavi i18n totali (50 nuove tra Marco e Paky), copertura IT=EN al 100%. Decisione architetturale: ogni workspace istanzia autonomamente i panel atomici (no shared singleton) — semplice e robusto, sync dati via SignalBus/AppState.
- 2026-05-16: Lezione delega parallela — Marco e Paky possono lavorare su `gui/i18n/strings.py` simultaneamente SE leggono il file prima di scrivere e usano nomi chiave non sovrapposti. Pattern `_try_import_workspace()` graceful in `main_window.py` permette dev parallelo senza ImportError al boot.
- 2026-05-16: **Fase 3 COMPLETATA** — commit `63a54ad`. Marco crea `gui/widgets/activity_bar.py` (126 LOC) — QFrame 56px verticale con 6 _ActivityButton, icone Unicode (⌖↗○▦✦⚙), stati idle/hover/active con bordo sinistro 3px #58a6ff, tooltip tradotti. API contract: `workspace_changed = pyqtSignal(int)`, `set_active(idx)`. Paky modifica `main_window.py` (261→306 LOC) — layout root `QHBoxLayout` con ActivityBar + colonna principale (TopBar+QStackedWidget), graceful try/except per import ActivityBar, `_switch_workspace(idx)` esteso per sync bidirezionale. Quality gate cross-validato: 6/6 switch da main_window aggiornano ActivityBar attivo + 6/6 click su ActivityBar cambiano QStackedWidget. Pattern di delega parallela funziona da 2 cicli — Marco+Paky con API contract pre-bloccato.
- 2026-05-16: **Fase 3.5 polish post visual check** — commit `53ced06` + `db21dd2`. Visual check live ha rivelato 3 difetti: (1) titoli QGroupBox tagliati in alto, (2) contrasto testi grigi #8b949e troppo basso, (3) PatternPanel "Pattern in Osservazione" + "Pulisci terminati" troncati orizzontalmente. Fix: dark.qss `margin-top: 10px→18px` + `top: -7px→2px` su QGroupBox::title; palette globale `#8b949e→#a8b1bb` (contrast ~7:1 AAA WCAG) + `#484f58→#6e7681` su 24 file; PatternPanel `_apply_i18n()` esteso per impostare title/btn via tr() con chiavi pattern.title="Pattern Attivi" / pattern.btn_clear="Pulisci". Commit successivo db21dd2 rimuove selettore "Tema" dal Settings (era placeholder disabilitato confuso, app supporta solo tema scuro) + cleanup file .tmp_* + .gitignore esteso.
- 2026-05-16: **Fase 4 COMPLETATA** — commit `7f06b27`. Marco crea 8 nuovi info widget atomici in `gui/widgets/info/` (1037 LOC totali): ConfidenceBar (barra 0-1 con threshold marker), BiDirectionalBar (split bull/bear), Heatmap (matrice correlazioni), PingIndicator (dot+latency+uptime), StatusDot (LED idle/active/error/loading con pulse), LiveLabel (QLabel con flash 100ms), FFTMini (spettro 120x44 con peak marker), NumericTable (QTableWidget pre-configurato monospace/color/sparkline). API uniforme `set_*(value)`. Esportati da `gui/widgets/info/__init__.py` (totale 13 info widget: 5 MVP + 8 Fase 4). Chiavi i18n aggiunte: bidir.bear / bidir.bull. Quality gate 8/8 import + 8/8 istanziazione headless + API set_* esercitata su tutti. Nessuna regressione su MainWindow.
- 2026-05-16: **Fase 5 COMPLETATA** — commit `7246e63` (estensione SignalBus) + `fae14d4` (emit + listener). Max estende `core/signal_bus.py` con 5 nuovi segnali + 5 dataclass + 5 emit helper (incluso side-effect `emit_ai_result` -> `regime_update`). Paky aggiunge emit nei moduli core (engine, orchestrator, risk_manager, auto_config — +98 LOC, 5/5 segnali coperti). Marco aggiunge listener nei panel atomici (ai_analysis +118 LOC, watchlist +56, positions +69, engine +80) + crea PortfolioPanel nuovo (183 LOC, Heatmap correlazioni). 9 chiavi i18n nuove per loop names + portfolio + total_pnl. Quality gate end-to-end emit→listener PASS per tutti i 5 segnali. **Pipeline live ora completa**: AutoConfig→ai_result→GUI aggiornata, RiskManager→kelly_update→Gauge, Engine 4 loop→heartbeat→StatusDot pulse, posizioni→correlation_update→Heatmap. PortfolioPanel non ancora integrato in nessun workspace (todo Fase 6 o successiva).
- 2026-05-16: **Visual check Fase 5 + Fase 5.5 polish PARZIALE** — commit `1b95788`. Visual check ha rivelato 3 problemi: (A) Workspace Ordini: bottoni Broker "Salva impostazioni"/"Testa connessione" clippati a sinistra + header tabella troncati ("STRUMENTO"→"STRUMENTC", DIR→"SE...", STATO→"PENDI..."), (B) Cruscotto: arricchimenti Fase 5 NON visibili (dashboard usa widget interni propri `_WatchlistPanel`/`_PositionsPanel` invece dei panel atomici di `gui/panels/`; EnginePanel orfano), (C) AI Analysis arricchito visibile in Analisi (Ctrl+3) — RegimePill+ConfidenceBar+BiDirectionalBar+Gauge Hurst/Kelly+FFTMini OK. **Fix A.2 committato** (header ResizeToContents). Marco fermato per chiusura utente prima di completare A.1 (splitter sizes), A.3 (padding bottoni), B (refactor Cruscotto).
- 2026-05-18: **Fase 5.5 CHIUSA**. Marco completa A.1+A.3+B in un colpo solo. (A.1) `order_ticket.py`: splitter [300,450,250]→[350,750,480], min-width form 280→330, min-width broker 200→420. (A.3) `broker_panel.py`: bottoni Save/Test ottengono `padding:6px 14px` + `min-height:32px` + `setSizePolicy(Expanding, Fixed)` — niente più clipping orizzontale anche con splitter ristretto. (B) `dashboard.py` -580 LOC netti (863→393): rimosse classi interne `_WatchlistPanel`/`_PositionsPanel`/`_AIPanel`; sostituite con i panel atomici reali Fase 5 (`WatchlistPanel`, `PositionsPanel`, `AIAnalysisPanel`, `EnginePanel`). Layout V-split a 3 sezioni a sinistra. (commit `1e20e83`).
- 2026-05-18: **Fase A CHIUSA — filo conduttore architetturale**. Visual check live ha rivelato 10 problemi che convergono in un'unica revisione architetturale: GUI senza filo, duplicazione tab Analisi/workspace Analisi, click watchlist non sync, gauge ferme (già fixato in commit precedente), chart non caricato, ordini macchinosi, scope Data poco chiaro. Utente definisce visione: **trader umano single-instrument (frontend) vs AI multi-instrument (backend) separati**. Piano in 5 fasi (A-E). Fase A completata in 2 step. **A.1** (commit `1a932cb`): `AppState.current_symbol` sorgente unica, listener su tutti i panel single-instrument (AI title sync, Chart info, OrderTicket combo pre-compilato), nuovo segnale `current_scan_symbol(str,str)` emesso da engine, chip simbolo in TopBar. **A.2+A.3** (commit `9c97abf`): Cruscotto trader-puro (rimosso tab Analisi duplicato, 1 solo tab Trading, aggiunta `_FundamentalsStrip` async con auto-refresh on current_symbol), workspace Analisi (Ctrl+3) trasformato in `AIObservatoryWorkspace` (SymbolScanPanel con 7 LED + EventLogPanel + SignalsTable + PerformanceFooter). `analysis.py` ridotto a wrapper alias per retro-compat. Decisione utente Q3: workspace Data eliminato, fundamentals integrati nel Cruscotto come strip 36px. Tempi: A.1 1 ciclo Paky, A.2+A.3 1 ciclo Marco (con API error a metà, ripreso da file persistiti). Restanti: Fase B (backend visibile polish), C (CandlestickChart integration), D (UX ordini auto-download), E (bug Pattern + cleanup duplicazione watchlist + rimuovi data_panel).
- 2026-05-18: **Fase 5.7 CHIUSA** — HelpIcon su ogni panel + dedupe Run AI Analysis. Visual check live Cruscotto: utente segnala (1) ridondanza "AI Analysis" presente 3 volte (tab name + titolo panel + bottone hardcoded `Run AI Analysis` in inglese in `gui/ui/ai_analysis_panel.ui:52`), (2) richiesta `?` info su ogni panel. Marco aggiunge HelpIcon programmaticamente (strategia b: `insertWidget` runtime, no modifica .ui) accanto al titolo dei 6 panel atomici (Watchlist, Positions, AI, Engine, Portfolio, Broker), ognuno con tooltip "cosa fa + come si usa" via 12 chiavi i18n nuove. Bottone AI passa da hardcoded "Run AI Analysis" a `tr("ai.btn_run")` → "▶ Analizza" / "▶ Analyze". Quality gate cross-validato: 6/6 panel hanno 1 HelpIcon ciascuno, bottone AI traduce correttamente IT↔EN, `_apply_i18n()` cablato su bus.qt.language_changed con try/except per test headless. Risolto anche fix yfinance 1.2.0→1.3.0 in commit separato `29f89a6` (HTTP 401 Invalid Crumb da Yahoo, ora curl_cffi gestisce fingerprint browser).
- 2026-05-18: **Fase 5.6 CHIUSA** — Cruscotto a tab. Visual check post-5.5 ha rivelato che il V-split a 3 panel atomici in colonna ~280px era illeggibile (header "CHANGE%" troncato in WatchlistPanel; "Lambdi/DKII/Drowdown" caption EnginePanel troncate; PositionsPanel sovraffollato con form ordine manuale + tabella trade recenti). Decisione utente: "chart sempre visibile, resto in tab, a gruppi correlati, un tab alla volta, almeno 50% schermo". **Layout finale**: WatchlistPanel sticky sinistra (300px), colonna destra V-stack con chart 50% / gauge strip 82px fissi / TabWidget 50% con **2 macro-tab**: "Trading" (Posizioni+Engine in QSplitter H 50/50) e "Analisi" (AI Analysis+Portfolio in QSplitter H 50/50). PortfolioPanel finalmente NON orfano (era listener di `correlation_update` ma senza container). `_GaugeStrip` separato da `_ChartArea` (chart sostituibile in futuro senza toccare le gauge). i18n: rimosse 4 chiavi `workspace.tab_*` flat, aggiunte 2 `workspace.tab_trading`/`workspace.tab_analysis`. Quality gate cross-validato Marco+Max: 2 tab, ognuno è QSplitter a 2 widget, tutti e 5 i panel atomici esposti come attributi, stretch verticali [50, 0, 50], i18n IT/EN funzionante.
- 2026-05-18: **Fix 4 bug Workspace Operativo CHIUSO** — commit `8595075`. Visual check post-Fase A ha rivelato 4 problemi sul Workspace Operativo (Ctrl+2). **A** `order_ticket._field_label`: setMinimumHeight(16) + spacing 6→8 (etichette font-size 10px+letter-spacing collassavano a 0 → overlap col widget sottostante). **B** `engine_panel.metricsFrame`: QSS generico `QFrame{}` veniva ereditato dai QLabel figli rendendo le caption illeggibili. Fix: scope `QFrame#metricsFrame{}` + `QLabel{background:transparent;border:none;}` esplicito + testo "Posizioni aperte"→"Pos. Aperte" abbreviato. **C** `positions_panel + .ui`: rimosso manualGroup (form ordine manuale duplicato — esiste già in OrderTicketWorkspace dopo refactor 65d5fce). Eliminati `_setup_form`, `_on_manual_trade`, `MANUAL_SYMBOLS`, import `OpenTradeCommand`. **D** Sync simbolo custom end-to-end: `order_ticket._symbol_combo.setEditable(True)` + `_on_combo_text_changed` (combo→AppState) + `_sync_combo_to_symbol` con `blockSignals` anti-loop; stesso pattern speculare in `backtest_panel._on_backtest_symbol_changed`. Simboli non in INSTRUMENTS (es. MSFT, AAPL) ora propagano correttamente combo Operativo→AppState→TopBar/Backtest e viceversa. Quality gate 7/7 PASS.
- 2026-05-20: **Fase C CHIUSA — chart integration** — commit `e43423d`. Utente sceglie Fase C tra le 5 opzioni di ripartenza. Marco sostituisce il placeholder `_ChartArea` in `dashboard.py` con `ChartPanel` reale (wrapper già pronto attorno a `CandlestickChart` pyqtgraph: candele+volume+MA+crosshair+zoom). Wiring fetch: `DashboardWorkspace` ascolta `current_symbol_changed` + carica al boot → `_fetch_chart_data()` async chiama `UniversalDataFeed().get_ohlcv(symbol, "1h", limit=400)` → `chart_panel.load_data()`. Pattern asyncio identico a `_FundamentalsStrip` (`asyncio.ensure_future` + check `loop.is_running()` per headless). Silent fail su rete/simbolo invalido → empty state. TF fisso 1h (selettore TF fuori scope). Un solo file modificato. Quality gate cross-validato Marco+Max: import+istanziazione+PatternsWorkspace intatto. `update_live_tick` non collegato (richiede emit dal bus, task futuro). Visual check live demandato all'utente al prossimo lancio.
- 2026-05-16: Lezione encoding — su PowerShell Windows serve sempre `$env:PYTHONIOENCODING="utf-8"` per stampare caratteri Unicode (▶, ⏸, →, ·, ecc.) in stdout dai test Python. Senza, errore `UnicodeEncodeError: 'charmap' codec`. Documentato per evitare falsi negativi nei quality gate.
- 2026-05-18: Lezione commit message PowerShell — `git commit -m @'...'@` here-string in PowerShell 5.1 NON gestisce bene i caratteri `+`, `<->`, `<-`, `&&` nel messaggio (PowerShell li interpreta come operatori e spezza in argomenti separati causando "pathspec did not match"). Workaround: scrivere il messaggio in file `.commit_msg_tmp.txt` UTF-8 e usare `git commit -F file`, poi `Remove-Item`. Più robusto del here-string per messaggi multi-linea con caratteri speciali.

## Lezioni apprese (permanenti)

- L'utente vuole densità informativa Bloomberg-grade, non solo styling. Le statusbar oggi mostrano "Pronto" e nascondono tutto ciò che il motore calcola — questo è il problema reale da risolvere.
- Quando dai consigli strategici, includi sempre il trade-off principale in 1 riga prima di proporre l'opzione consigliata.
- L'utente apprezza le AskUserQuestion con opzione "Recommended" chiara — non galleggiare, prendi posizione.
- **Encoding Windows trap**: PowerShell `Get-Content -Raw` su questo Windows legge i file con encoding CP1252 di default. Se poi salvi con `Set-Content -Encoding utf8`, ogni byte multibyte UTF-8 (▶ ⌖ € → · ✦ ⚙) viene corrotto in mojibake (â€¢, âœ, etc). Per find&replace su file UTF-8: usare ESCLUSIVAMENTE Python con `Path.read_text(encoding="utf-8")` + `write_text(encoding="utf-8")`. Mai PowerShell per text manipulation di file con caratteri non-ASCII. Documentato dopo incidente 2026-05-16 (Fase 3.5 polish).

## Consigli dati all'utente

- 2026-05-14: Consigliato Path A (Qt + qdarkstyle + info widgets ricchi) come opzione a basso rischio se TradingIA è uso personale. Path B (Flutter+FastAPI) raccomandato solo se il target è prodotto distribuibile.

## Task aperti

- [x] **Fase 1.5 — Polish difetti gate review** — completata 2026-05-15 (commit f92bda2)
- [x] **Fase 1.6 — Internazionalizzazione (IT default + EN opzionale)** — completata 2026-05-16 (Step 1-4 al 100%, commit `97e40d9`, `2357c01`, `b8239b8`, `810387a` per Step 4)
- [x] **Fase 2 — 5 workspaces rimanenti** — completata 2026-05-16 (commit `810387a`)
- [x] **Fase 3 — ActivityBar verticale** — completata 2026-05-16 (commit `63a54ad`)
- [x] **Fase 3.5 — Polish post visual check** — completata 2026-05-16 (commit `53ced06` + `db21dd2`)
- [x] **Fase 4 — 8 info widget atomici** — completata 2026-05-16 (commit `7f06b27`)
- [x] **Fase 5 — Arricchimento panel + 5 segnali SignalBus** — completata 2026-05-16 (commit `7246e63` + `fae14d4`)
- [x] **Fase 5.5 — Polish post visual check** — completata 2026-05-18 (commit `1b95788` per A.2 + commit corrente per A.1+A.3+B)
- [x] **Fase A — Filo conduttore architetturale** — completata 2026-05-18 (commit `1a932cb` + `9c97abf`)
- [x] **Fix 4 bug Workspace Operativo (A-B-C-D)** — completato 2026-05-18 (commit `8595075`)
- [x] **Fase C — Chart integration** — completata 2026-05-20 (commit `e43423d`)
- [ ] **PUNTO DI RIPARTENZA 2026-05-21**: scegliere prossima fase tra:
   - Fase 6 — Fix stati pulsanti via AppState (Buy/Sell setEnabled engine_running, broker Test loading, quick-pick) — ~2h
   - Fase E — Pulizia: fix Pattern recognition + unifica 2 watchlist + rimuovi data_panel
   - Fase B — Backend visibile polish: indicatore "engine sta scansionando X"
   - Fase D resto — auto-download al primo click simbolo non in cache + rimozione data_panel
   - Bridge tick-live: collegare `ChartPanel.update_live_tick()` al SignalBus (emerso da Fase C, non ancora cablato)
- [ ] Validazione statistica pattern recognition (Tom + Chloe) — task strategico pre-esistente, da rivalutare dopo refactor GUI completo

## Workflow

All'inizio di ogni sessione:
1. Leggi questa memoria + CLAUDE.md
2. Identifica task aperti rilevanti
3. Saluta l'utente con "[Max]" e proponi continuità sui task aperti

Alla fine di ogni task:
1. Aggiungi 1-3 righe in "Decisioni recenti" (rimuovi le più vecchie se >20)
2. Aggiungi a "Lezioni apprese" solo se hai scoperto un'insidia non documentata
3. Aggiorna "Task aperti" (sposta a "Decisioni recenti" se completati)
